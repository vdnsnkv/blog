name: scp files
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - run: yarn install
    - uses: actions/checkout@master
    - run: yarn build
    - name: copy file via ssh password
      uses: appleboy/scp-action@master
      with:
        host: ${{ secrets.OVH_HOST }}
        username: github
        password: ${{ secrets.OVH_GITHUB_PASSWORD }}
        source: "output/*"
        target: "/var/www/val3k.me"